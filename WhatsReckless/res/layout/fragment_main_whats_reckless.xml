<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.whatsreckless.MainWhatsReckless$PlaceholderFragment" >

    <TextView
        android:id="@+id/txtCurrentState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/webView"
        android:layout_toLeftOf="@+id/btnPullCurrentState"
        android:text="@string/current_state" />

    <Button
        android:id="@+id/btnPullCurrentState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/txtCurrentState"
        android:text="@string/pull_location"
        android:onClick="pullState" />

    <WebView
        android:id="@+id/webView"
        android:layout_width="288dp"
        android:layout_height="372dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true" />

    <TableLayout
        android:id="@+id/recklessInfoTable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/webView"
        android:layout_alignLeft="@+id/webView"
        android:layout_alignRight="@+id/webView"
        android:layout_alignTop="@+id/webView" />

</RelativeLayout>